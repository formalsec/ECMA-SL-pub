function main() {

  l := 100;
  i := 100;
  while (i > 0){
    str := extern str_symbol("str");
    extern assume(s_len(str) <  l);
    
    len := s_len(str);

    if(len = 50){
      print "len == 50"
    };

    i := i - 1
  };

  return 0
}
