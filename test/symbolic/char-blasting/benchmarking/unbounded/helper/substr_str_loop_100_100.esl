function main() {

  l := 100;
  while (l > 0){

    str := extern str_symbol("str");
    extern assume(s_len(str) < 10);
    
    sub_str := s_substr(str, 30, 50);

    l := l - 1
  };

  return 0
}
